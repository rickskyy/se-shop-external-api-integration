# RTF Market External API Integration module

### Install
Install pip3, venv

`pip3 install virtualenv`

`virtualenv my_project`

http://docs.python-guide.org/en/latest/dev/virtualenvs/

`pip3 install -r requirements.txt`